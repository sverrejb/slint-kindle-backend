[package]
name = "slint-kindle-clock-suspend"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
slint = { version = "1.16", default-features = false, features = ["compat-1-2", "std", "renderer-software"] }
slint-backend-kindle = { path = "../.." }
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }

[build-dependencies]
slint-build = "1.16"
