[package]
name = "slint-kindle-buttons"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
slint = { version = "1.16", default-features = false, features = ["compat-1-2", "std", "renderer-software"] }
slint-backend-kindle = { path = "../.." }

[build-dependencies]
slint-build = "1.16"
